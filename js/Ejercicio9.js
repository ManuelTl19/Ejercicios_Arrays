/**
 * 9. Filtrar números pares
Dado un arreglo de números, retorna solo los números pares usando filter().
 */

let numeros=[3,4,6,8,7,5];
const pares=numeros.filter(num=>num%2==0);

console.log(pares);