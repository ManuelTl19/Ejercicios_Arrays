/**
 * Ordenar alfabéticamente
Dado un arreglo de strings, ordena el arreglo en orden alfabético.
 */

let palabras=["Jose", "Manuel", "Aida", "Pablo", "Alan"];
let ordenar=palabras.sort();
console.log(ordenar);