/**
 * 10. Multiplicar por 2
Crea una función que multiplique cada número de un arreglo por 2 usando map().
 */

let numeros=[4,4,2,4,6,8,5,3];

const duplicarArreglo=numeros.map(num=>num*2);

console.log(duplicarArreglo);